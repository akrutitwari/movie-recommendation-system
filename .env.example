# TMDB v3 API key. Required for scripts/download_tmdb.py (builds the catalog)
# and recommended for the backend shell (live streaming-platform lookups).
# Get one free at https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_v3_api_key
